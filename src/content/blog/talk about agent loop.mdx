---
title: '对Agent loop底层逻辑的初次拆解'
description: 'first try agent loop'
pubDate: '3 1 2026'
language: 'Chinese'
---

最近在工作中需要理解一些自家产品的设计架构，有一部分是关于agent loop的内容。

我做过的agent玩具都比较简单，按我之前的理解，我只需提供给agent一个可用的tools列表，然后它自己决定要不要使用就ok，但实际上可能会存在更复杂的情况。

如果没有agent loop那么只会存在单次工具调用（工具调用就是输出结果）

这次leader的要求让我有了一个机会彻底去拆解agent loop的底层运行逻辑，摆脱一知半解的状态！

例如一个天气查询agent

```typescript
import 'dotenv/config';
import OpenAI from 'openai';
import type { ChatCompletionMessageParam, ChatCompletionTool } from 'openai/resources/chat/completions';

// 初始化客户端
const openai = new OpenAI({
  // 若没有配置环境变量，请用阿里云百炼API Key将下行替换为：apiKey: "sk-xxx",
  // 新加坡和北京地域的API Key不同。获取API Key：https://help.aliyun.com/zh/model-studio/get-api-key
  apiKey: process.env.DASHSCOPE_API_KEY,
  // 以下是北京地域base_url，如果使用新加坡地域的模型，需要将base_url替换为：https://dashscope-intl.aliyuncs.com/compatible-mode/v1
  baseURL: "https://api.deepseek.com/v1",
});

// 定义工具列表
const tools: ChatCompletionTool[] = [
  {
    type: "function",
    function: {
      name: "get_current_weather",
      description: "当你想查询指定城市的天气时非常有用。",
      parameters: {
        type: "object",
        properties: {
          location: {
            type: "string",
            description: "城市或县区，比如北京市、杭州市、余杭区等。",
          },
        },
        required: ["location"],
      },
    },
  },
];

// 天气查询参数类型
interface WeatherArgs {
  location: string;
}

// 模拟天气查询工具
const getCurrentWeather = (args: WeatherArgs): string => {
  const weatherConditions = ["晴天", "多云", "雨天"];
  const randomWeather = weatherConditions[Math.floor(Math.random() * weatherConditions.length)];
  const location = args.location;
  return `${location}今天是${randomWeather}。`;
};

// 封装模型响应函数
const getResponse = async (messages: ChatCompletionMessageParam[]) => {
  const response = await openai.chat.completions.create({
    model: "deepseek-chat",
    messages: messages,
    tools: tools,
  });
  return response;
};

const main = async () => {
  const input = "北京天气咋样";
  let messages: ChatCompletionMessageParam[] = [
    {
      role: "user",
      content: input,
    }
  ];

  let response = await getResponse(messages);
  let assistantOutput = response.choices[0].message;

  // 确保 content 不是 null
  if (!assistantOutput.content) assistantOutput.content = "";
  messages.push(assistantOutput);

  // 判断是否需要调用工具
  if (!assistantOutput.tool_calls) {
    console.log(`无需调用天气查询工具，直接回复：${assistantOutput.content}`);
  } else {
    // 进入工具调用循环
    while (assistantOutput.tool_calls) {
      const toolCall = assistantOutput.tool_calls[0];
      const toolCallId = toolCall.id;
      const funcName = toolCall.function.name;
      const funcArgs = JSON.parse(toolCall.function.arguments) as WeatherArgs;
      console.log(`正在调用工具 [${funcName}]，参数：`, funcArgs);

      // 执行工具
      const toolResult = getCurrentWeather(funcArgs);

      // 构造工具返回信息
      const toolMessage: ChatCompletionMessageParam = {
        role: "tool",
        tool_call_id: toolCallId,
        content: toolResult,
      };
      console.log(`工具返回：${toolMessage.content}`);
      messages.push(toolMessage);

      // 再次调用模型获取自然语言总结
      response = await getResponse(messages);
      assistantOutput = response.choices[0].message;
      if (!assistantOutput.content) assistantOutput.content = "";
      messages.push(assistantOutput);
    }
    console.log(`助手最终回复：${assistantOutput.content}`);
  }
};

// 启动程序
main().catch(console.error);

```

我们可以看到整个执行过程，这就是一个完整的agent loop + function Calling架构

```
user输入 -> agent决定是否调用工具 -> 需要调用 -> 把调用完成的结果加入上下文，再次确定是否需要调用(直到agent认为不需要调用) -> agent输出结果
```

简单总结上方代码的逻辑就是这样

```typescript
while (!done) {
  think()
  act()
  observe()
}
```

生产情况可能会更复杂，例如timeout之类的防止死循环。不过能够理解底层逻辑就ok！